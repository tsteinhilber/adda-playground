<%= form_for(value_list) do |f| %>
  <% if value_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(value_list.errors.count, "error") %> prohibited this value_list from being saved:</h2>

      <ul>
      <% value_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
