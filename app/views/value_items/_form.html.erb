<%= form_for([value_item.value_list, value_item]) do |f| %>
  <% if value_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(value_item.errors.count, "error") %> prohibited this value_item from being saved:</h2>

      <ul>
      <% value_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value_list_id %>
    <%= f.text_field :value_list_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :position %>
    <%= f.number_field :position %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
